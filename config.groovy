// Defaults
indicator = 'local'

environments {
    local {
        indicator = 'local'
    }

    test {
        indicator = 'test'
    }

    dev {
        indicator = 'dev'
    }

    prod {
        indicator = 'prod'
    }
}